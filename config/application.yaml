server:
  host: "0.0.0.0"
  port: 8080
  worker_id: 1

database:
  database_url: "postgresql://stars:stars123@localhost:5432/stars"
  min_connections: 5
  max_connections: 20
  idle_timeout_seconds: 30

redis:
  url: "redis://127.0.0.1:6379"

login:
  issuer: "https://accounts.share-stars.com"
  audience: "https://www.share-stars.com/api"
  expire_seconds: 3600

mfa:
  issuer: "https://accounts.share-stars.com"
  audience: "https://www.share-stars.com/chanllege"
  expire_seconds: 3600

sms:
  account_sid: "123"
  auth_token: "123"
  from_phone: "18877776666"
  status_callback_url: "https://api.share-stars.com/api/notify/sms"

email:
  from_email: "noreplay@share-stars.com"


